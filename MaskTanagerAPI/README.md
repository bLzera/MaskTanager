# API

## Docker-compose
Esse diretório tem um docker-compose.yml, que containeriza o runtime necessário para rodar a API, e o banco de dados com a qual a API vai se comunicar.

### how to build
Para buildar o projeto, basta rodar ```docker compose up -d --build```
